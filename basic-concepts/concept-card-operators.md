## 🧠 TARJETA DE ESTUDIO – OPERADORES

### ¿Qué son los operadores?

Los operadores son **símbolos que permiten realizar operaciones** sobre uno o más valores.

En términos simples:
👉 son la forma en que el programa **trabaja con los datos**.

---

### Tipos de operadores más comunes

#### 1️⃣ Aritméticos

Se usan para cálculos:

* `+` suma
* `-` resta
* `*` multiplicación
* `/` división
* `%` módulo (resto)

Clave de entrevista:

> No todos los operadores aritméticos devuelven enteros.

---

#### 2️⃣ De comparación

Comparan valores y **devuelven booleanos**:

* `==` / `===` (igualdad)
* `!=` / `!==` (desigualdad)
* `<`, `>`, `<=`, `>=`

Idea clave:
👉 **Comparar no es asignar**
(`=` asigna, `==` compara)

---

#### 3️⃣ Lógicos

Trabajan con valores booleanos:

* `&&` AND
* `||` OR
* `!` NOT

Se usan principalmente en:

* Condicionales
* Validaciones
* Control de flujo

---

### Precedencia de operadores

Los operadores **no se evalúan todos igual**.

Regla práctica:

> Primero aritméticos, luego comparación, luego lógicos.

Cuando hay duda:
👉 **usar paréntesis** mejora claridad y evita bugs.

---

### Errores comunes en entrevistas

* Confundir `=` con `==`
* No entender qué devuelve una expresión
* Comparar objetos o strings incorrectamente
* Asumir el orden de evaluación sin paréntesis

---

### Buena práctica

Priorizar **legibilidad** sobre “código ingenioso”.

---

### Frase que suma puntos

> “Una condición clara vale más que una condición corta.”

---

Decime cuál sigue y la armamos.
